extends ../layouts/layout.pug

block variables
	-var title = "Каталог оборудования — STEAMSYS";
	-var bodyCl = "ss-wrapper";

block content
	include ../sections/_header.pug
	main.ss-main-inner
		include ../sections/_hero-catalog.pug
		include ../sections/_catalog-nav.pug
		//- Остальные секции (about-section / catalog / usage / documents) —
		//- подключаем следующим шагом.
	include ../sections/_footer.pug
